-- Baseline: the schema as shipped in database/schema.sql.
--
-- This file deliberately contains no statements. schema.sql already creates
-- every table with CREATE TABLE IF NOT EXISTS, so an existing database is
-- already at this version -- recording it here just gives the runner a known
-- starting point. Every change from now on gets its own numbered file.
SELECT 1;
